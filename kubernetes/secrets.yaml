apiVersion: v1
kind: Secret
metadata:
  name: longevity-coach-secrets
  namespace: longevity-coach
type: Opaque
data:
  # Base64 encoded secrets
  SUPABASE_SERVICE_ROLE_KEY: "your-base64-encoded-service-role-key"
  OPENAI_API_KEY: "your-base64-encoded-openai-key"
  JWT_SECRET: "your-base64-encoded-jwt-secret"
  ENCRYPTION_KEY: "your-base64-encoded-encryption-key"
  DATABASE_PASSWORD: "your-base64-encoded-db-password"
  REDIS_PASSWORD: "your-base64-encoded-redis-password"
  MONITORING_API_KEY: "your-base64-encoded-monitoring-key"
  ALERTING_WEBHOOK_URL: "your-base64-encoded-webhook-url"












